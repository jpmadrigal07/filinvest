import { Block } from "payload/types";

const CompanyPoliciesTitle: Block = {
  slug: "company-policies-title",
  fields: [
    {
      type: "text",
      name: "title",
    },
    {
      type: "text",
      name: "subtitle",
    },
  ],
};

export default CompanyPoliciesTitle;
